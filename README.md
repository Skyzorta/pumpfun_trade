# PumpFun Trade

## Overview
This project is for a trading agent that automates trading activities for various assets.

## Installation
To install the required dependencies, run:

```bash
pip install -r requirements.txt
